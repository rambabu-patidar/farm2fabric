import React from "react";

import Modal from "../../UI/Modal";

const ItemDetail = (props) => {
	return (
		<Modal onClose={props.onClose}>
			<h1>Rambabu Patidar</h1>
		</Modal>
	);
};

export default ItemDetail;
